jq >= 1.9.1
httpx >= 0.28.1
fastapi[standard] >= 0.116.1
sse-starlette >= 3.0.2
# Extra deps for the Vertex (Gemini + Anthropic-on-Vertex) servers/driver.
# Kept separate from requirements.txt so the existing OpenAI setup is untouched.
google-genai>=2.14.0
anthropic[vertex]>=0.120.0
